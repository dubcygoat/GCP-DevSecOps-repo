steps:
  - name: gcr.io/cloud-builders/mvn
    entrypoint: 'mvn'
    args: ['verify', 'sonar:sonar','-Dsonar.host.url=https://sonarcloud.io','-Donar.organization=dubcygoat', '-Dsonar.projectKey=dubcygoat_gcp-git', '-Dsonar.token=${_SONAR_TOKEN}']
options:
       logging: CLOUD_LOGGING_ONLY